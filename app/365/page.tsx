export default function Page365() {
  return (
    <main style={{ minHeight: "100vh", background: "black", color: "white", padding: 24 }}>
      <h1 style={{ fontSize: 22, margin: 0 }}>365</h1>
      <p style={{ opacity: 0.7 }}>Spend engine (transactions + rolling vendor summary) — coming next.</p>
    </main>
  );
}
